# Frontend MVP

React + Vite frontend skeleton for ISRO Team SIRIus.

## Run locally

```bash
cd frontend
npm install
npm run dev
```

Set `VITE_API_BASE_URL=http://localhost:8000` if the backend runs elsewhere.
